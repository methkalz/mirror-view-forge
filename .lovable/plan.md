

## تعديلات بطاقة جائزة العشرة الأوائل

تعديلان بسيطان على `src/components/PrizeEntryCard.tsx`:

### 1. خانة رقم الهاتف
- **الحالة الحالية**: القيمة الابتدائية للحقل تحوي `962` (أو نص مشابه يظهر بلون كامل ولا يختفي عند التركيز).
- **التغيير**:
  - تفريغ القيمة الابتدائية: `useState('')` (تأكيد).
  - استبدال الـ `placeholder` الحالي `+962 7X XXX XXXX` بـ **`0526444414`**.
  - الـ placeholder بطبيعته يظهر بلون خفيف ويختفي تلقائياً عند الكتابة، لذلك لا حاجة لمنطق إضافي.
  - تأكيد أن لون الـ placeholder خفيف (سيُضاف `::placeholder` style بلون `rgba(148,163,184,0.35)` لضمان الوضوح على الخلفية الداكنة).

### 2. رسالة النجاح بعد الإرسال
- **الحالة الحالية**: `toast.success('تم — حظًا موفقًا! 🍀')`
- **التغيير**: `toast.success('مبروك يا كبير')`

### الملفات المتأثرة
- `src/components/PrizeEntryCard.tsx` (تعديلان: قيمة الـplaceholder، ونص رسالة النجاح + ستايل placeholder).

