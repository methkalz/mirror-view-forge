

# 3 تعديلات سريعة

## 1. رفع توقيع المطور في الشريحة الثالثة (`src/game/renderer.ts`)
العناصر حالياً عند `h*0.82 → h*0.96` — تتداخل مع نقاط الشرائح (dots).
- رفع الكتلة كاملة: الخط الفاصل العلوي `h*0.74`، "تطوير" `h*0.78`، "مثقال زيدان" `h*0.82`، "METHKAL ZIDANE" `h*0.855`، الخط الفاصل السفلي `h*0.88`

## 2. زر شاشة التحميل (`src/components/GameLoader.tsx`)
- سطر 282: تغيير `ابدأ المغامرة ⚔️` إلى `التالي` (بدون أيقونة)

## 3. زر شاشة إدخال الاسم (`src/components/NameEntry.tsx`)
- سطر 383-384: تغيير `ابدأ المعركة` + أيقونة السيف إلى `أنا مُستعد` (بدون أيقونة)

