

# إلغاء الغيوم + نجوم متلألئة قليلة

## التغييرات

### 1. `src/game/renderer.ts` — إزالة رسم الغيوم + تحسين النجوم
- **حذف** كود رسم الغيوم بالكامل (سطور 101-143)
- **تعديل** رسم النجوم (سطور 90-97):
  - تقليل العدد من 60 → 15 نجمة
  - رسمها كنجوم حقيقية (شكل نجمة 4 رؤوس) بدل مربعات
  - تلألؤ أبطأ وأنعم مع تنوع في الحجم (1-3px)
  - إضافة توهج خفيف (glow) حول كل نجمة باستخدام `shadowBlur`

### 2. `src/game/engine.ts` — إزالة تحديث الغيوم
- **حذف** دالة `initClouds` (سطور 83-96)
- **استبدال** `clouds: initClouds(w, h)` → `clouds: []`
- **حذف** حلقة تحديث الغيوم (حوالي سطر 592-600)
- **حذف** `g.clouds = initClouds(...)` من `resetGame`

ملف واحد للنجوم، ملف واحد لتنظيف الغيوم. لا تغييرات أخرى مطلوبة.

