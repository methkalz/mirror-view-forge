

# تحديد عرض 3 أسماء فقط في لوحة المتصدرين

## التغيير
**ملف واحد**: `src/components/Leaderboard.tsx`

### ما سيتغير
- تغيير `maxHeight` للقائمة من `calc(35vh)` إلى قيمة ثابتة تناسب 3 صفوف فقط: كل صف ~40px + gap 6px = ~**132px**
- تعديل شرط التمرير التلقائي من `entries.length <= 5` إلى `entries.length <= 3` — ليبدأ التمرير عند وجود أكثر من 3 أسماء
- الحفاظ على كل التأثيرات الموجودة: التمرير التلقائي، التدرج السفلي، الميداليات

