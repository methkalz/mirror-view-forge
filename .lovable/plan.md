

# إعادة ضبط الشريحة الثالثة — Elegant Minimal مع مساحة للشعار

## الوضع الحالي (سطر 5727–5868 في `renderer.ts`)
العناصر الحالية من أعلى لأسفل:
- `S K Y F A L L` عند `h*0.22` — خط 48px monospace + تدرج ذهبي
- خط فاصل ذهبي عند `h*0.27`
- `SURVIVAL` عند `h*0.32` — خط 14px
- `"هل أنت مستعد؟"` عند `h*0.42` — خط 15px
- زر `"يلا يلا"` عند `h*0.52`
- أفضل نتيجة عند `h*0.66`
- توقيع المطور عند `h*0.92`

**المشاكل**: العناصر متكدسة في النصف العلوي، مساحات التنفس غير متوازنة، وعنوان SKYFALL النصي سيُستبدل بشعار (صورة) لاحقاً.

## التغييرات — `src/game/renderer.ts` → `renderTutorialSlide3`

### 1. تجهيز مساحة الشعار (أعلى الشاشة)
- **حذف** عنوان `S K Y F A L L` النصي والخط الفاصل تحته و `SURVIVAL`
- **إضافة مساحة محجوزة** عند `h*0.15 → h*0.38` للشعار المستقبلي (صورة)
- حالياً: رسم placeholder خفيف — مستطيل شفاف بحدود ذهبية رفيعة مع نص `"LOGO"` صغير بالمنتصف حتى تُضاف الصورة لاحقاً

### 2. إعادة توزيع العناصر عمودياً (توازن أفضل)

```text
┌──────────────────────────┐
│                          │
│     [ مساحة الشعار ]      │  h*0.15 → h*0.38
│                          │
│                          │
│    هل أنت مستعد؟          │  h*0.48  (16px Tajawal)
│                          │
│    ┌──────────────┐      │
│    │   يلا يلا     │      │  h*0.55  (زر)
│    └──────────────┘      │
│                          │
│       🏆 420             │  h*0.65  (إذا > 0)
│                          │
│                          │
│       ─── ◆ ───          │  h*0.82  (خط فاصل)
│        تطوير             │  h*0.85
│    مثقال زيدان            │  h*0.89
│   METHKAL ZIDANE         │  h*0.92
│       ─── ◆ ───          │  h*0.95
└──────────────────────────┘
```

### 3. تحسينات التصميم
- **"هل أنت مستعد؟"**: تكبير لـ `16px`، شفافية `0.45` (أوضح قليلاً)
- **زر "يلا يلا"**: تكبير النص لـ `20px`، عرض الزر `200px`، ارتفاع `48px` — أكثر بروزاً
- **أفضل نتيجة**: خط `14px` مع مسافة أوضح
- **توقيع المطور**: نقله لأعلى قليلاً (`h*0.85–0.95`) مع خط فاصل ذهبي فوقه وتحته لتأطيره كبلوك مستقل أنيق

### ملف واحد: `src/game/renderer.ts` — إعادة كتابة الدالة (~140 سطر → ~100 سطر)

